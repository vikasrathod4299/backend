// import { Router } from 'express';
// import {followUser, getFollowers, getFollowing, getRecentFollowUpdates, getUsersWhichAreNotFollowing} from "../controllers/follower"
// import { verifyToken } from '../middlewares/auth';
// const followRoutes = Router();

// followRoutes.get('/:userId/followers', getFollowers);
// followRoutes.get('/:userId/following', getFollowing);
// followRoutes.get('/:userId/not-following',  getUsersWhichAreNotFollowing);
// followRoutes.get('/recent-follow-updates', getRecentFollowUpdates);
// followRoutes.post('/follow', verifyToken, followUser);
// // router.post('/unfollow', followerController.unfollowUser);





// export default followRoutes;